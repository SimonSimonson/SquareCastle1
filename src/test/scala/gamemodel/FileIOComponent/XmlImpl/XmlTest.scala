/*
import gamemodel.FileIOComponent.XmlImpl.Xml
import gamemodel.model.MapComponent.Map
import org.scalatest.{Matchers, WordSpec}

import scala.util.Success

class XmlTest extends WordSpec with Matchers {

  "Json" when {
    "new" should {
      val xml = new Xml
      "undo command" in {
        xml.save(new Map(4,4)) should be (true)
        //xml.load should be (null)
        xml.fieldToXml(new Map(4,4)) should not be (null)

      }

    }
  }
}
 */